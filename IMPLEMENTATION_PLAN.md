# План калибровки шаблонов печати листа А4 в LIGA OS (v2.2.4)

## Цель задачи
Довести до швейцарского идеала печатные формы формата А4:
1. **Официальный Акт гидравлического испытания 16 бар / 24ч** — строгая посадка ровно на **1 страницу А4** без обрезания и без паразитной 2-й страницы.
2. **Исполнительный Инженерный Паспорт объекта** — идеальная **двухстраничная раскладка** (Лист 1: Паспортные данные, протокол 16 бар, гарантия, подписи; Лист 2: Фотофиксация трасс с рулеткой, узел ввода, регламент отделочников).

---

## Совет трёх экспертов

### 1. Архитектор
- **Акт 16 бар**:
  - Геометрия страницы `@page { size: A4 portrait; margin: 8mm 10mm; }`.
  - Защита от разрывов: `page-break-inside: avoid; break-inside: avoid;` для всех секций.
  - Компактная верстка фото (высота 110px), таблиц и подписей, гарантирующая высоту страницы до 1000px при 96dpi.
  - Векторный QR-код верификации подлинности (SVG, без сторонних библиотек, 100% офлайн).
  - Защитная гильош-сетка / элегантный водяной знак герба Лиги.
- **Инженерный Паспорт**:
  - Явный разделитель `.page-break { page-break-before: always; break-before: page; }`.
  - Страница 1: Шапка с золотым гербом Лиги + Паспорт объекта + Протокол 16 бар + Предупреждение отделочникам + Двухуровневая гарантия + Подписи и факсимиле.
  - Страница 2: Мини-шапка 2-го листа + Фотофиксация 4 зон (узел FAR, манометр 16 бар, трассы в стенах, трассы в полу) + Правила безопасности + Заверение инженера.

### 2. Разработчик
- Редактирование файла `js/pdf_engine.js`.
- Сохранение 100% обратной совместимости со всеми селекторами и текстами тестов `test_official_pressure_act.py` и `test_passport_honesty.py`.
- Добавление удобной плавающей панели печати с кнопкой закрытия окна (`window.close()`) и хоткеями.

### 3. Инженер по качеству (QA)
- Создание теста `tests/test_a4_print_calibration.py`.
- Проверка эмуляции печати `page.emulate_media(media="print")`.
- Проверка суммарной высоты одностраничного Акта 16 бар и наличия `page-break` в Паспорте.
- Прогон всего сьюта тестов (все 29 тестов со 100% PASS).
- Обновление версий до `v2.2.4` в `sw.js` и фиксация в `DECISION_JOURNAL.md`.
