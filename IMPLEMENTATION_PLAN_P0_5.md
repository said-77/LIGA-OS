# План реализации P0-5: Честность сетевых и инженерных статусов

## 1. Описание задачи и архитектурный контекст
По результатам независимого аудита GPT и требованиям канона (`HANDOFF_AND_SPEC_RU.md`) зафиксированы принципы технологической честности:
1. **Запрет AI-Washing**: Набор статических экспертных правил (проверка рентабельности, выдерживание опрессовки 16 бар, проверка сухого затвора) не является "нейросетевым AI-агентом". Документ и интерфейс обязаны честно именовать модуль: **«Инженерный экспресс-аудит (экспертные правила)»**.
2. **Честность сетевых статусов**: PWA LIGA OS позиционируется как автономная система для работы в подвалах и новостройках. Однако функции распознавания речи (Web Speech API) и передача внешних ссылок цифровых расписок требуют подключения к сети. Разграничение «🟢 100% Автономно (офлайн)» и «🌐 Требуется подключение к сети» должно быть явно видно мастеру.

---

## 2. Предлагаемые изменения по компонентам

### 2.1. Интерфейс (`index.html`)
#### [MODIFY] [index.html](file:///h:/PROEKTY/МОЁ_GPT_ПРОСТРАНСТВО/LIGA-M_OSIM/LIGA-OSIM/LIGA_OSIM/index.html)
1. **Кнопка и модальное окно аудита**:
   - Кнопка в шапке `#btn-ai-audit-top`: иконка заменяется на статусную инженерную линейку/щит `📐`, `title="Инженерный экспресс-аудит (экспертные правила)"`.
   - Заголовок `#modal-ai-audit`: `📐 Инженерный экспресс-аудит объекта`.
   - Подзаголовок: *«Анализ финансового баланса и рисков по инженерным правилам Лиги Мастеров (DIN 1988, СП 30.13330)»*.
   - Текст лоадера: *«Проверка инженерных правил, статуса опрессовки и баланса...»*.
   - Кнопка: `✓ Зафиксировать инженерные выводы в журнал`.
2. **Маркировка сетевой зависимости в окне голосового ввода (`modal-voice`)**:
   - Добавление информационного бейджа под микрофоном:
     `🌐 Голосовой ввод речи требует подключения к интернету. При работе в экранированных монолитах и подвалах используйте текстовое поле ввода ниже.`
3. **Маркировка в окне цифровой расписки (`modal-verify-receipt`)**:
   - Добавление пометки о передаче ссылки через Telegram/мессенджеры.
4. **Бейджи сетевой доступности на экране «Финансы»**:
   - Пункт резервного копирования JSON: бейдж `🟢 100% Офлайн (в памяти телефона)`.
   - Внешние ссылки (портфолио, 3D-визитка): бейдж `🌐 Онлайн (требуется сеть)`.
5. **Индикатор в шапке приложения**:
   - Статус: `ТАШКЕНТ • 🟢 БАЗА ОФФЛАЙН`.

---

### 2.2. Логика приложения (`js/app.js`)
#### [MODIFY] [app.js](file:///h:/PROEKTY/МОЁ_GPT_ПРОСТРАНСТВО/LIGA-M_OSIM/LIGA-OSIM/LIGA_OSIM/js/app.js)
1. В методе `runAiAudit()`:
   - Обновление текстовых формулировок выводов.
   - Метод `saveAiAuditNotes()` выводит тост: `✓ Выводы инженерного аудита зафиксированы в истории объекта!`.
2. В методе `startVoiceRecording()`:
   - Проверка `navigator.onLine`. Если устройство не в сети, мягко выводить предупреждение:
     *«⚠️ Нет связи с интернетом: голосовое распознавание может быть недоступно. Введите фразу вручную в поле ниже.»*.

---

### 2.3. Обновление Service Worker (`sw.js`)
#### [MODIFY] [sw.js](file:///h:/PROEKTY/МОЁ_GPT_ПРОСТРАНСТВО/LIGA-M_OSIM/LIGA-OSIM/LIGA_OSIM/sw.js)
- Обновление версии кэша до `liga-os-v1.4.5-p0`.

---

## 3. План верификации и автоматические тесты

### 3.1. Новый автотест Playwright: `tests/test_honesty_network_and_audit.py`
1. **Проверка честности аудита**:
   - Клик по кнопке аудита в шапке (`#btn-ai-audit-top`).
   - Проверка заголовка окна: наличие фразы «инженерный экспресс-аудит» и отсутствие термина «ИИ-Аудитор» / «ИИ анализирует».
   - Проверка сохранения результатов аудита и текста тоста.
2. **Проверка сетевой прозрачности голосового ввода**:
   - Открытие модального окна голоса (`#modal-voice`).
   - Проверка наличия пояснения о сетевой зависимости Web Speech API и возможности ручного ввода.
3. **Проверка бейджей офлайн/онлайн на экране Финансов**:
   - Проверка наличия бейджей автономности для локальных данных и онлайн-бейджей для внешних ссылок.
4. **Проверка индикатора в шапке**:
   - Проверка индикатора `🟢 БАЗА ОФФЛАЙН` или аналогичной честной маркировки.

### 3.2. Полный регрессионный прогон
- Запуск `python -m pytest tests/ -v` (все 5 тестов: сметы, паспорт, клиентский режим, бэкап/восстановление, сетевая честность).
- Запуск `tests/test_receipt_and_both_themes.py`.
- Запуск `tests/test_voice_guide_duplicate_e2e.py`.
