# ТЕХНИЧЕСКОЕ ЗАДАНИЕ, ПЕРЕДАТОЧНЫЙ АКТ И КАНОН (HANDOFF & SPEC) — LIGA OS
**Дата фиксации**: 20 сентября 2026 г.  
**Проект**: LIGA OS (Операционная Система Инженерного Мастера)  
**Репозиторий**: [https://github.com/said-77/LIGA-OS](https://github.com/said-77/LIGA-OS)  
**Хэш последнего коммита**: `01ba010` -> готовится коммит v2.0.4 (навигация, реестр контактов, Swiss Audio Web API)  
**Статус продукта**: Функциональный продукт высшей инженерной надежности (выполнены P0-1 – P0-5, Акт 16 бар, 10-летняя история мастера, непрерывный голос Keep-Alive, реактивность переключения объектов и CRUD, навигация `.screen-top-bar` с возвратом по кнопке «Назад к объекту» и крестику, постоянный реестр контактов Лиги, офлайн аудио-синтезатор Swiss Audio Web API; **19 из 19 сквозных тестов Playwright успешно пройдены**).

---

## 1. СТРОГИЕ ПРИНЦИПЫ И ИСПРАВЛЕНИЕ ОШИБОК ПОЗИЦИОНИРОВАНИЯ (ПО АУДИТУ GPT)

На основе строгого независимого аудита GPT и указаний владельца проекта зафиксированы следующие обязательные правила:

1. **Честность хранения данных и резервного копирования**:
   - Запрещено называть Telegram «вечным, безлимитным и неломающимся облаком».
   - Веб-приложение (PWA) без серверного бэкенда и секретных токенов бота НЕ может автоматически отправить файл в «Избранное».
   - **Утвержденная реальная архитектура**:
     - Приложение генерирует файл резервной копии `liga_backup_YYYY-MM-DD.json`.
     - Вызывается стандартный системный интерфейс `navigator.share({ files: [...] })` (меню «Поделиться» операционной системы смартфона). В этом меню Улугбек сам выбирает Telegram, WhatsApp, Почту или сохранение на диск.
     - Если системный Web Share API недоступен (например, на старых браузерах) — выполняется прямое скачивание файла в загрузки телефона.
     - Запрещено обещать «шифрованные защищенные .liga архивы», пока нет физического шифрования. На текущем этапе используется надежный валидируемый JSON с проверкой структуры, даты и схемы.
     - Запрещено заявлять «восстановление за 2 секунды». Процедура восстановления обязана быть контролируемой: валидация схемы файла, показ модального окна с метаданными (сколько объектов, сколько чеков, дата создания) и явное подтверждение мастером перед заменой данных.

2. **Формирование стоимости и тарифов (Смета)**:
   - Запрещено назначать жесткие коммерческие расценки (например, «1 500 000 сум за санузел») в коде от своего имени. Ценообразование — исключительная прерогатива мастера Улугбека.
   - **Утвержденное решение**:
     - В коде создается прозрачный конфигуратор тарифов (объект `tariffSettings`), доступный для редактирования и калибровки.
     - Стартовые расчетные значения явно маркируются в интерфейсе: `«Базовый ориентир (требует утверждения Улугбеком)»`.
     - Все параметры на экране (санузлы, водорозетки, инсталляции, iBox, трапы, теплый пол) обязаны математически строго участвовать в расчете итоговой суммы либо быть убраны.

3. **Честность документов и паспорта объекта**:
   - Запрещено автоматически писать «Опрессовка 16 бар пройдена, 24 часа без падения давления», если мастер физически не внес акт гидроиспытаний и фото манометра с датой.
   - До момента внесения реальных замеров документ обязан иметь статус **«ЧЕРНОВИК / ИСПЫТАНИЯ НЕ ЗАФИКСИРОВАНЫ»**.
   - Кнопка формирования паспорта честно именуется: **«Печать / Экспорт в PDF»**, так как генерация выполняется через оптимизированный HTML-диалог печати браузера.

4. **Изолированный «Клиентский режим» (Client Presentation Mode)**:
   - Это не просто сокрытие элементов через CSS (`display: none`).
   - Это отдельный безопасный экран/контекст показа, в модель отображения которого физически не передаются внутренние зарплатные долги бригаде, себестоимость материалов с базара и личные заметки мастера.

5. **Честность технологических статусов**:
   - Запрещено называть систему «100% офлайн», если Web Speech API (распознавание речи) или отправка расписок через мессенджеры требуют подключения к сети. Разграничение «Офлайн-база» и «Функции, требующие сеть» должно быть явным.
   - Запрещено называть набор статических правил «AI-Аудитором». Функция должна честно именоваться: **«Инженерный аудит по экспертным правилам»**.

6. **Единственный источник истины**:
   - Единственным источником истины является репозиторий GitHub. Все документы на Рабочем столе — это удобные копии для владельца.

---

## 2. АКТУАЛЬНЫЙ СТАТУС ПРОВЕРОК КОДОВОЙ БАЗЫ

### Реально проверено (Evidence / Факты):
- Запуск приложения на локальном HTTP-сервере.
- Отображение и адаптивность 5 табов нижней панели на мобильном экране (`iPhone 14 Pro`, 393×852).
- Открытие модальных окон: голосовой ввод, предупреждение о дубликате чека, цифровая расписка с URL-параметрами, памятка со скриптами диалогов.
- Отсутствие синтаксических ошибок в JavaScript-файлах при базовой загрузке.
- Удаление служебной папки `.playwright-cli` и её блокировка в `.gitignore`.
- ✅ **P0-1 ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ТЕСТАМИ**: Санузлы включены в математическую формулу расчета сметы (`costPerBathroom: 1 500 000 сум`), тарифы вынесены в настраиваемый конфигуратор `tariffSettings` с сохранением в `localStorage`, добавлена маркировка «Базовый ориентир (требует утверждения Улугбеком)» и модальное окно настройки тарифов. Реализована строгая валидация (запрет отрицательных значений, курс USD > 0, блокировка ошибочного ввода). Версия офлайн-кэша Service Worker повышена до `liga-os-v1.4.1-p0`. Физически подтверждено автоматическим тестом Playwright (`tests/test_estimate_math.py`, результат: PASSED, 100% assertions, тест зафиксирован в Git).
- ✅ **P0-2 ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ТЕСТАМИ**: Честный Инженерный Паспорт объекта с защитой от фиктивной опрессовки. Кнопка переименована в «Печать / Экспорт в PDF (Паспорт объекта)», добавлен индикатор готовности на дашборде. Реализована строгая двухфакторная проверка опрессовки (16 бар + фото манометра): без фото формируется честный «ЧЕРНОВИК» с неактивированной гарантией и соответствующим факсимиле; при наличии фото формируется подтвержденный документ с живой фотофиксацией. Версия Service Worker обновлена до `liga-os-v1.4.2-p0`. Физически подтверждено тестом `tests/test_passport_honesty.py` (PASSED).
- ✅ **P0-3 ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ТЕСТАМИ**: Изолированный «Клиентский режим» (Client View). Переключатель `👁️`/`🔒` в шапке с сохранением в `localStorage`. Надежная двухуровневая изоляция: декларативная скрывает классы `.master-only`, а программная подставляет прочерки `—` в долги бригаде и бонусы дизайнерам, исключает оптовые цены и чеки, подставляя бейджи спецификации `.mat-badge-spec`, блокирует вывод сводного портфеля других объектов и вызов служебных модальных окон (скрипты мастера, тарифы, платежи, чеки, аудит). Версия Service Worker обновлена до `liga-os-v1.4.3-p0`. Физически подтверждено тестом `tests/test_client_mode.py` (PASSED).
- ✅ **P0-4 ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ТЕСТАМИ**: Надежное локальное резервное копирование и восстановление (Full JSON Backup & Restore). Снимок всех таблиц IndexedDB (`sites`, `materials`, `checklists`, `finances`, `passports`, `tariffSettings`), нативный вызов `navigator.share({ files: [...] })` с фолбэком на локальный файл `liga_backup_YYYY-MM-DD.json`. Строгий валидатор схемы (`validateBackup`), модальное окно `#modal-backup-manager` с предпросмотром метаданных (дата, кол-во объектов, чеков, тарифов) и явным подтверждением перед перезаписью. Версия Service Worker обновлена до `liga-os-v1.4.4-p0`. Физически подтверждено тестом `tests/test_backup_restore.py` (PASSED).
- ✅ **P0-5 ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ТЕСТАМИ**: Честность сетевых и инженерных статусов. Полное искоренение «AI-washing»: модуль переименован в «Инженерный экспресс-аудит (экспертные правила)» с четкой привязкой к техническим нормам (DIN 1988, СП 30.13330, гидроиспытания 16 бар) и детерминированному анализу. Статус `🟢 БАЗА ОФФЛАЙН` зафиксирован в шапке. На экране «Финансы» добавлены бейджи `🟢 100% Офлайн` для локального бэкапа и `🌐 Онлайн` для внешних ресурсов. В окне голосовой диктовки и цифровой расписки добавлены предупреждения о сетевой зависимости (Web Speech API и отправка ссылок через мессенджеры) с обеспечением ручного ввода при отсутствии связи. Версия Service Worker обновлена до `liga-os-v1.4.5-p0`. Физически подтверждено тестом `tests/test_honesty_network_and_audit.py` (5 тестов из 5 PASSED).

### Выявленные дефекты и что НЕ проверено:
- ❌ **НЕ ПРОВЕРЕНО**: Поведение Web Speech API в реальных условиях полного отсутствия сети на физическом iOS/Android (в коде добавлена проверка `navigator.onLine` и ручной ввод).
- ❌ **НЕ ПРОВЕРЕНО**: Длительное хранение больших объемов фото в IndexedDB на реальных смартфонах.

---

## 3. ОЧЕРЕДЬ РЕАЛИЗАЦИИ: БЛОК P0 (СТРОГО ПОШАГОВО)

Каждый пункт выполняется изолированно, с обязательной проверкой тестами перед переходом к следующему!

### 1. P0-1: Прозрачная и тестируемая смета [ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ✅]
- [x] Включить санузлы в математический расчет.
- [x] Вынести все расценки в настраиваемый конфигуратор тарифов (`tariffSettings`).
- [x] Добавить пометку: «Базовый ориентир (требует утверждения Улугбеком)».
- [x] Написать автоматический тест (`test_estimate_math.py`), подтверждающий пересчет сметы при любом изменении санузлов. (Тест пройден со 100% успехом).

### 2. P0-2: Честный Инженерный Паспорт объекта [ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ✅]
- [x] Переименовать действие в «Печать / Экспорт в PDF (Паспорт объекта)».
- [x] Добавить реактивный индикатор готовности паспорта на дашборде.
- [x] Проверять наличие реального статуса опрессовки (`site.pressTestPassed`) И фото манометра (`photos.pressure`).
- [x] Если опрессовка не зафиксирована или нет фото — формировать паспорт строго со статусом «ЧЕРНОВИК / ИСПЫТАНИЯ НЕ ПРОВОДИЛИСЬ», предупреждением о неактивированной гарантии и факсимиле черновика.
- [x] При подтвержденной опрессовке с фото — формировать официальный статус «✓ 16 БАР ПРОЙДЕНО (ПОДТВЕРЖДЕНО)» с живой фотофиксацией.
- [x] Написать сквозной Playwright-тест (`tests/test_passport_honesty.py`), проверяющий все 4 состояния опрессовки и фотофиксации (пройден со 100% успехом).

### 3. P0-3: Изолированный «Клиентский режим» (Client View) [ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ✅]
- [x] Реализовать отдельный режим презентации для заказчика/дизайнера (переключатель `👁️`/`🔒` в шапке).
- [x] Гарантировать отсутствие в клиентском представлении внутренних долгов бригаде, закупочных цен и служебных пометок мастера.
- [x] Заменить закупочные цены стройматериалов на представительские бейджи европейской спецификации.
- [x] Изолировать сводный финансовый радар портфеля других объектов мастера в Ташкенте.
- [x] Блокировать открытие служебных модальных окон мастера при активном режиме заказчика.
- [x] Написать сквозной Playwright-тест (`tests/test_client_mode.py`), проверяющий полную изоляцию интерфейса и данных (пройден со 100% успехом).

### 4. P0-4: Надежное локальное резервное копирование и восстановление [ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ✅]
- [x] Экспорт полного снимка базы в `liga_backup_<date>.json` со всеми объектами, материалами, чеками и тарифами.
- [x] Вызов системного `navigator.share()` с фолбэком на прямое скачивание файла.
- [x] Строгий валидатор схемы перед восстановлением, исключающий повреждение базы битыми или чужими файлами.
- [x] Модальное окно предпросмотра метаданных (дата, кол-во объектов, чеков) и запрос подтверждения мастера перед заменой данных.
- [x] Написать сквозной Playwright-тест (`tests/test_backup_restore.py`), проверяющий валидацию и полный цикл восстановления (пройден со 100% успехом).

### 5. P0-5: Честность сетевых и инженерных статусов [ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ✅]
- [x] Заменить «AI-Аудитор» на «Инженерный экспресс-аудит (экспертные правила)».
- [x] Разграничить офлайн-индикаторы и функции, требующие сеть (Web Speech API, внешние ссылки).
- [x] Написать сквозной Playwright-тест (`tests/test_honesty_network_and_audit.py`), подтверждающий корректность формулировок и бейджей.

### 6. P0-6: Полноценная светлая тема (Light Mode) с глубоким контрастом [ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ✅]
- [x] Реализация высококонтрастного стиля для яркого солнца Ташкента (угольный текст `#0F172A`/`#090e17`, светлая керамика, золотые акценты `#9A6A1D`, отсутствие блеклости).
- [x] Переключатель темы `☀️ / 🌙` с сохранением в `localStorage`.
- [x] Полноценный Playwright-тест в общем сьюте (`tests/test_light_theme.py`), проверяющий контрастность элементов при ярком свете (пройден со 100% успехом).

---

## V. ОФИЦИАЛЬНЫЙ АКТ ОПРЕССОВКИ 16 БАР / 24 ЧАСА [ВЫПОЛНЕНО И ПОДТВЕРЖДЕНО ✅]
- [x] Проектирование бланка А4 «Официальный Акт гидравлических испытаний 16 бар» с юридической резолюцией допуска к стяжке пола (`PLAN_OFFICIAL_ACT_16BAR.md`).
- [x] Реализация изолированного метода генерации в `js/pdf_engine.js` (`generatePressureAct`).
- [x] Добавление кнопок вызова на дашборде (`#btn-generate-act`), в экране чек-листа стяжки (`#btn-act-screed-print`) и в модальных окнах опрессовки и фотопривязок.
- [x] Фиксация честного статуса: *«Результаты испытания внесены и подтверждены мастером (Хакимовым Улугбеком) на объекте по показаниям опрессовочного оборудования Лиги Мастеров»*.
- [x] Сквозной тест Playwright `tests/test_official_pressure_act.py` (успешно пройден, общий сьют: 12 из 12 тестов).

---

## VI. 10-ЛЕТНЯЯ ИНЖЕНЕРНАЯ ИСТОРИЯ И ЭЛИТНЫЙ UI [ВЫПОЛНЕНО И ПРОТЕСТИРОВАНО ✅]
- [x] Модернизация схемы IndexedDB до v2: 3 новых защищенных локальных хранилища:
  - `site_timeline_events` — Append-Only хроника инженерных этапов (аудит, черновой монтаж, опрессовка 16 бар, стяжка, чистовая, сервис);
  - `brigade_payouts` — учет выплат помощникам бригады (ФИО, роль, сумма в сумах, эквивалент в USD, расчетный курс, способ оплаты, описание работ);
  - `installed_equipment` — паспорта европейских узлов (FAR, Rehau, Geberit, Caleffi) с заводской гарантией 10-50 лет.
- [x] Реализация интерфейса «История» (6-я вкладка навигации) с переключателем 3 подвкладок («Хронология», «Бригада», «Оборудование») и модальными окнами добавления.
- [x] Полная изоляция в Client View: выплаты бригаде и служебные финансовые поля скрыты от заказчика.
- [x] Элитный UI мирового уровня: «дышащий» логотип в шапке, золотой неоновый индикатор активного таба, бейджи уведомлений на иконках «Склад» и «Контроль», плавное появление карточек, тактильные нажатия, пульсирующий долг, планшетная верстка `@media (min-width: 768px)`, стили печати `@media print`.
- [x] Устранен критический дефект: добавлен отсутствовавший метод `toggleMaterialStatus()` и универсальный `copyToClipboard()` с fallback.
- [x] Обновлен Service Worker кэш до `liga-os-v2.0.0-history`.
- [x] Сквозной тест Playwright `tests/test_engineering_history_10years.py` (успешно пройден, общий сьют: 13 из 13 тестов).

---

## VII. СТРОГИЙ ПОДГОТОВИТЕЛЬНЫЙ СТАТУС SUPABASE [ОЖИДАНИЕ ШЛЮЗА ДОПУСКА ⏳]
- [x] Архитектурная готовность: 3 режима, Zero-Knowledge шифрование, принцип «Невидимый Supabase», 7 условий шлюза допуска.
- [x] Запрет на текущем этапе: никаких проектов/ключей Supabase, никаких API, никаких фейковых кнопок или заглушек в UI, никаких пробных периодов 5-7 дней или оплат.
- [ ] Ожидание наступления 7 условий шлюза допуска перед началом кодирования сетевого взаимодействия.


