# Итоговый отчет о задаче P0-1: Прозрачная и тестируемая смета LIGA OS

## 1. Что отправлено в GitHub

1. **Коммит №1 (`c77705f`)**:
   - Математическая формула сметы с параметром «Санузлы» (`costPerBathroom: 1 500 000 сум`).
   - Настраиваемый конфигуратор `tariffSettings` с сохранением в `localStorage`.
   - Маркировка «Базовый ориентир (требует утверждения Улугбеком)».
   - Модальное окно калибровки тарифов мастером.
   - Отправлен в удаленный репозиторий `main -> main` (хэш `c77705f`).

2. **Коммит №2 (корректировки надежности и фиксация тестов)**:
   - Строгая валидация тарифов (`validateTariffField`):
     - Запрет отрицательных значений.
     - Проверка на корректное целое число.
     - Курс доллара USD строго положительный (> 0, минимум 1000).
     - Запрет немого превращения ошибочного ввода в 0; вывод наглядного предупреждения мастеру.
     - Форма снабжена атрибутом `novalidate` для единообразной валидации на всех смартфонах с русскоязычными уведомлениями.
   - Обновление офлайн-кэша Service Worker:
     - `CACHE_NAME = 'liga-os-v1.4.1-p0'` в `sw.js` (гарантирует моментальное обновление приложения у всех ранее установивших PWA мастеров).
   - Раскладка и фиксация тестов в репозитории:
     - Правила `test_*.py` и `verify_*.py` удалены из `.gitignore`.
     - Все тесты перемещены в постоянный каталог `tests/` и добавляются в Git.
   - Обновление `HANDOFF_AND_SPEC_RU.md` и `DECISION_JOURNAL.md` (Запись 006).

---

## 2. Какие тесты реально находятся в Git

В каталоге [`tests/`](file:///h:/PROEKTY/МОЁ_GPT_ПРОСТРАНСТВО/LIGA-M_OSIM/LIGA-OSIM/LIGA_OSIM/tests) зафиксированы:
1. [`tests/test_estimate_math.py`](file:///h:/PROEKTY/МОЁ_GPT_ПРОСТРАНСТВО/LIGA-M_OSIM/LIGA-OSIM/LIGA_OSIM/tests/test_estimate_math.py) — сквозной тест сметы (расчет санузлов, вилка цен, сохранение кастомных тарифов, персистентность в `localStorage` после перезагрузки страницы, валидация отрицательных значений и курса USD, сброс к базовым ориентирам).
2. `tests/test_receipt_and_both_themes.py` — тест расписок и переключения светлой/темной темы.
3. `tests/test_voice_guide_duplicate_e2e.py` — тест голосового ввода, памятки мастера и защиты от повторных чеков.
4. `tests/verify_and_screenshot.py` — тестовый сценарий снятия скриншотов.

---

## 3. Что реально проверено (Факты и Evidence)

Запущен расширенный автотест Playwright:
```bash
python -m pytest tests/test_estimate_math.py -v -s
```
**Результат**: `PASSED` (100% проверок успешно):
- [x] Расчет при 2 санузлах: `17 700 000 – 22 125 000 сум` ($1 372 – $1 715).
- [x] Расчет при увеличении до 3 санузлов: `19 200 000 – 24 000 000 сум` ($1 488 – $1 860).
- [x] Расчет при уменьшении до 1 санузла: `16 200 000 – 20 250 000 сум` ($1 256 – $1 570).
- [x] Честный статус: `Базовый ориентир (требует утверждения Улугбеком)`.
- [x] Сохранение кастомного тарифа (санузел 2 000 000 сум) -> пересчет в `18 700 000 – 23 375 000 сум`.
- [x] Перезагрузка страницы (`page.reload()`) -> тариф восстанавливается из `localStorage`, сумма рассчитана по нему.
- [x] Защита от ввода отрицательной цены (-500 000) -> модалка не закрывается, диалог выдает ошибку, значение блокируется.
- [x] Защита от нулевого курса доллара (0) -> модалка не закрывается, диалог требует положительный курс, значение блокируется.
- [x] Кнопка «Сброс» возвращает базовые тарифы и исходную сумму `17 700 000 – 22 125 000 сум`.

---

## 4. Что пока НЕ проверено (Честный статус готовности)

- ❌ **P0-2 (Паспорт объекта)**: еще не переименован в «Печать / Экспорт в PDF» и еще выводит 16 бар без проверки факта реальной опрессовки и фото.
- ❌ **P0-3 (Клиентский режим)**: еще не изолирован от внутренних финансов бригады и себестоимости с базара.
- ❌ **P0-4 (Резервное копирование)**: валидация схемы JSON-бэкапа и системный вызов `navigator.share()` в реальных сценариях.
- ❌ **Голосовой ввод на шумной стройке без интернета**: на мобильном железе Web Speech API в офлайне зависит от встроенного языкового пакета ОС телефона.
